# Production build config for the frontend.
# Frontend and API are served from the same origin (nginx serves the static
# build at "/" and proxies "/api" to the backend), so leave the API base
# empty to use relative URLs. This keeps the bundle domain-agnostic.
VITE_API_URL=
